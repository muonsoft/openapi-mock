openapi: "3.0.0"
paths:
  /entity:
    get:
      responses:
        default:
          content:
            application/json:
              schema:
                type: object
                required:
                  - id
                properties:
                  id:
                    type: integer
