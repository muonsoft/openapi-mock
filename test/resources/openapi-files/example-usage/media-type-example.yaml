openapi: "3.0.0"
info:
  title: 'test'
  version: 1.0.0
paths:
  /content:
    get:
      responses:
        200:
          description: 'test'
          content:
            application/json:
              schema:
                type: object
                properties:
                  key:
                    type: string
                    enum:
                      - generatedValue
              example:
                key: 'exampleValue'
